function coloredItems(colors) {
  for (let index = 0; index < colors.length; index++) {
    console.log(index, colors[index]);
  }
}

var colors = ["red", "orange", "yellow", "green", "blue", "purple", "pink"];
coloredItems(colors);
